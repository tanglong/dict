using UnityEngine;
using UnityEngine.UI;
using System.Collections;
using longame.config;

public class LoginController : MonoBehaviour {

    void Awake()
    {
        DictInfoC_Reader.getInstance();
    }
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
